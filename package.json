{
  "name": "agent-office",
  "version": "1.0.0",
  "description": "AgentOffice — virtual AI office with autonomous agents",
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "build:wallet": "esbuild src/wallet-app.jsx --bundle --outfile=public/js/wallet.js --format=iife --target=es2020 --define:process.env.NODE_ENV=\\\"production\\\" --minify --loader:.jsx=jsx"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "konnekt-wallet": "^0.2.3",
    "socket.io": "^4.7.5",
    "three": "^0.183.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
